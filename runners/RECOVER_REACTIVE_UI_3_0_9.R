# FANTASY MODEL 3.0.9 - REACTIVE UI RECOVERY
# Sleeper refresh is already lightweight/model-first. This launcher starts the
# patched app where optional Trade Center controls cannot crash the Shiny session.
source("runners/RUN_DYNASTY_APP_MODEL_FIRST.R")
