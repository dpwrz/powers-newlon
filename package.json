{
  "name": "powers-newlon",
  "private": true,
  "version": "3.0.1",
  "scripts": {
    "build": "npm --prefix web install --no-audit --no-fund && npm --prefix web run build && rm -rf dist && cp -R web/dist dist"
  }
}
